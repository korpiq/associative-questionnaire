# Testing

This repository writes automated tests through the [`executable-specs`](../.agents/skills/executable-specs/SKILL.md) skill.

Use that skill as the primary source for:

- test layout
- Gherkin authoring rules
- step-structure conventions
- CLI/build/deployment coverage guidance

## Verification

- Run `npm run check`.
- Run `npm test`.
