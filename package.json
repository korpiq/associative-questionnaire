{
  "name": "associative-questionnaire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "check": "tsc --noEmit --project tsconfig.json",
    "generate": "tsx src/cli/generate-questionnaire-html.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "^6.3.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.0",
    "vitest": "^4.0.4",
    "yaml": "^2.8.1"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "zod": "^3.24.2"
  }
}
