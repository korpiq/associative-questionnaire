# TODO

- for free text answers, list all different ones without a visual line telling how many there are, as there are usually just one of each, but show a number of how many of each there are, just in case it's more
- prepare a showcase for how correct vs incorrect answers of different types are reported – a container with prefilled answers. It can be used for manual visual verification. `npm test:visual`
- an ssh installer with configurable install path under user's home on the remote host
